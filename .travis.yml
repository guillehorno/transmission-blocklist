sudo: false
language: minimal
branches:
  only:
    - master # build master branch only
script:
  - ./blocklist-build.sh
deploy:
  provider: releases
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: false
  file: "blocklist.gz"
    on:
    tags: true
